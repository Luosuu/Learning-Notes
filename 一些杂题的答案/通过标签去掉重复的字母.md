```
#include <iostream>
#include <stdio.h>
#include <string.h>
using namespace std;

int main() {
    char t[50];
    int flag=0;
    scanf("%s",t);
    int len = 0;
    while( *(t+len) !='\0')len++;
    for(int i = 0;i<len;i++)
    {
        if(t[i]!=t[i+1])
        {
            if(flag==0)cout<<t[i];
            flag = 0;
        }
        else if((t[i]==t[i+1])&&flag==0)
        {
            cout << t[i];
            flag =1;
        }
    }
}
// a - '\0'
```
